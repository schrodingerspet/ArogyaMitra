import "../src/index.css";

const preview = {
  parameters: {
    layout: "centered",
    controls: { expanded: true },
    backgrounds: {
      default: "dark",
      values: [{ name: "dark", value: "#071226" }],
    },
    a11y: { test: "todo" },
  },
};

export default preview;
